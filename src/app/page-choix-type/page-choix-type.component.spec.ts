/*
import { async, ComponentFixture, TestBed } from "@angular/core/testing";

import { ChoixTypeIncidentComponent } from "./page-choix-type.component";

describe("ChoixTypeIncidentComponent", () => {
    let component: ChoixTypeIncidentComponent;
    let fixture: ComponentFixture<ChoixTypeIncidentComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ ChoixTypeIncidentComponent ]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(ChoixTypeIncidentComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it("should create", () => {
        expect(component).toBeTruthy();
    });
});
*/
